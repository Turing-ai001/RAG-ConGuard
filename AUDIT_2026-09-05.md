# 代码与结果审计报告（2026-09-05）

> 审计对象:本地快照 `69c6cd6`(即服务器代码,109 文件哈希一致)+ `experiments/final_protocol` 全部结果。
> 方法:读实现(源码行号) + 结果文件统计 + **本地复算**(特征 JSONL + risk_learner.pkl 重放选择器/删除行为)+ 服务器重跑 B0(10 条 test / 10 条 clean_val,当前代码与主表 10/10 一致)。
> 约定:每问题标 **仅正文错误 / 实现错误 / 证据不足 / 暂未确认**;找不到的写"未找到",不按项目说明反推。未擅自修复;修复方案见 §7。

---

## P0-1 联盟划分规则;外部支持是否恒为零;control 是否退化为 impact

**事实链(全部有代码+数据依据)**:

1. **联盟划分规则**:候选联盟 = 图 **正边连通分量**。`graph/build.py:73-80` `coalition_nodes("signed")` 只用 `pos_edges`(`signed_semantic` 时语义边并入 pos_edges,分量语义同);「最终联盟」= `fp/selectors.py:23-28` `is_coalition`:size≥2 且 (cohesion>0 或 impact>0)(注释:等价于 Score(C)=α·Density+β·I−γ·IndepSupp,α=β=γ=1,单点豁免)。
2. **外部支持恒为零(数学必然)**:`coalition/analyze.py:40,58-59` 定义 `external_corroboration = external_pos / size`,而 `external_pos = _count_cross(pos_set, comp)` = **跨联盟正边数**。正边连通分量按定义**不存在跨分量正边**——任何正边连接的节点必属同一分量。∴ **corr ≡ 0 是定义级恒等式,不是数据巧合**。
3. **数据验证(4704 条联盟记录,全部 splits)**:

| split | 联盟数 | corr>0 条数 | corr>0 比例 |
|---|---|---|---|
| train | 409 | 0 | 0.0000 |
| val | 132 | 0 | 0.0000 |
| test | 141 | 0 | 0.0000 |
| clean_trn | 2690 | 0 | 0.0000 |
| clean_val | 1332 | 0 | 0.0000 |

   `control == impact` :train/val/test 合计 **682/682 = 100.0%**(`fp/pipeline.py:76` control = impact×(1−min(corr,1)) → corr=0 ⇒ control≡impact;exact_cci_val_H16.json 中 `rho_control_vs_exact == rho_impact_vs_exact` 逐项相等,同证)。
4. **具体查询案例(val,nq,test347)"when did the us dollar leave the gold standard"**:联盟 test347-0/1/2(三篇 `nq:poisoned:test347:…`,poisoned_ratio=1.0),`cohesion=0.6667, external_corroboration=0.0, external_refute=0.0, impact=0.92, control=0.92`——corr=0、control==impact 实例。
5. **推论**:论文/协议反复出现的判别维度「投毒联盟 = 高度内聚 + **外部佐证缺失** + 常被外驳;良性多源簇 = 被佐证」中,**"外部佐证缺失"维度(及 M5 的 −γ·IndepSupp 项)对所有联盟恒真,不提供任何判别信息**;M5 三维 Score 实际 = Density + I(二维);control 与 impact 完全相关。

**分类**:**实现错误**(特征定义与联盟发现方式不相容导致判别维度失效)+ **仅正文错误**(论文"externally uncorroborated / corroborated benign cluster"表述描述的机制未实质存在)。**注**:外驳维度(`external_refute`)不受此影响——负边不参与连通分量,可跨分量非零(案例该值为 0.0 为数据巧合)。

**待核查标记(按你要求,确认后再改)**:联盟定义、外部佐证公式——代码事实已如上确认;正文改动与公式细节留待你拍板(§7.1)。

---

## P0-2 推理期是否使用标准答案 / 攻击目标 / 投毒标记

**事实链**:

1. **候选答案(影响公式的 A 集)**:`fp/pipeline.py:41-54` = 与查询 BGE 最相似 top-2 claim 文本,**target-free**;注释明示"2026-08-25 v1.1 修正,旧实现用 benchmark correct/incorrect 是推理期 oracle 泄漏"。
2. **特征计算**:`fp/pipeline.py:56-77` 全部只依赖 docs/claims/edges/sims/cand_ids;**gold/attack_target 只写入 features 记录,用于事后 main_state 匹配(`fp/runner.py:282-300`),不进入 selectors/risk 分数**。`selectors._apply` 只用 `["claims","size","cohesion","impact",…]` 六特征,`LearnedRisk.predict_proba` 输入列固定为 size/cohesion/corroboration/refute/impact/control(`defense/risk.py:22-23`)→ **无 gold/投毒标记泄漏路径**。
3. **B4/B6/exact CCI**:`enrich_features.py:41-63` target-free 重算;`exact_cci.py:83-84` 用 rec["sims"]/cand_ids(同源 top-2)→ target-free ✓。
4. **例外(需你裁决)**:`fp/adaptive.py` **全部环节**(基础池正例、val 评测、挖矿)传 `answers=[correct, incorrect]` 作为候选答案(adaptive.py:168/207/246/273→`variant_group_risk`→sims/impact)。攻击者视角模拟可辩护,但论文"without knowing the attack target"的声明范围未明确 → **若声明覆盖自适应部分则不一致**。
5. **训练标签(允许的)**:`poisoned_ratio≥0.8`(runner.py:95-111)来自**投毒标注**——只用于训练/诊断,非推理输入 ✓。

**分类**:主评估管线 **已验证合规(target-free)**;自适应环节 **实现错误或仅需文档声明——二选一,由你定**(未找到说明自适应"攻击者知道目标"的文字在论文正文/协议中 → 证据不足→暂列待核查)。

---

## P0-3 S0/S2 实际怎样删除与重新加入文档

**事实链**(`fp/selectors.py` 全文):

1. **S0(select_s0,τ)**:risk≥τ 的联盟 → 其成员 claim 所在文档全删;`refused = len(kept)==0`。**没有重新加入**。
2. **S2(select_s2, λ, τ=0.0)**:按风险降序遍历联盟;`risk_i < τ` → break;**若删除该联盟后剩余文档数 < floor=max(1,⌊λ·n⌋) → break(该联盟及其后全部保留)**。语义 = "删除前做保留下限检查,不删则停止",即**不是"重新加入",也不是"重新计算风险"**(风险只算一次,`_risks` 在 `_apply` 内一次)。协议 §6(S2=贪婪重建+保留下限,严禁空上下文)与实现一致。
3. **执行真相(与调参不一致)**:`run_main_eval.sh` 主表覆盖 `G1:{"selector":"s2","lam":0.2}`,但 METHODS 默认 G1 为 `{"selector":"s1","tau":0.25}`,合并后 **G1 = S2@λ0.2 **且 τ=0.25 残留**;**tune.json 扫描的 S2 是 τ=0.0**。本地复算:test 上 S2(λ0.2,τ0.25) avg_removed=**1.667** = predictions G1 的 1.667(而 τ0.0 版=2.183,λ0.6 版=0.450)→ 主表 G1 确为 τ0.25 变体。clean 脚本的 G2=H(λ0.2,τ=0.0)(G2 无默认 τ)→ clean 表的"G2"与主表"G1"**不是同一操作点**(见 P1-3)。
4. **S1 从未出现在主表**(tune 后只跑 s0/s2——选中的 s2 与作者的 s0)。

**分类**:S2 语义本身 **实现正确、与协议 §6 一致**;但 **主表 G1 的执行点 ≠ 调参选中的点(τ 残留)** = **实现错误(参数合并缺陷)**;"重新加入"若出现在论文措辞中则为 **仅正文错误**(需核对正文——「S2 是否重新加入/重新计算」按你所问:实现未如此,博士未见 S2 正文措辞 → **待核查标记**)。

---

## P1-1 数据划分与选参是否符合冻结协议

**事实链**:

1. **划分来源**:`scripts/make_splits.py` = 投毒 sorted→Random(20260825).sample→60/20/20;clean = Random(20260826? **SEED+1**)→200/100/300;assert 无交集 ✓。服务器 splits 数字复核一致。**论文正文/卡:"split queries 70/30"**(cards M7、RAG_论文框架_初稿.md:353)——70/30 实为旧 eval_p3 的"插入序前 70%/后 30%"(PROJECT_AUDIT.md:75,139,已作废)→ **仅正文错误**。
2. **scaler 拟合范围**:`stage_train`(`runner.py:114-146`)池 = train 投毒联盟 + clean_trn 干净联盟,`LearnedRisk.fit` 仅在此池上 fit(scaler+MLP)→ **符合协议 §3/§8(只 fit train)** ✓。
3. **G0/G1 选参规则与执行(tune.py:125-135)**:
   - 代码规则:可行集(refusal≤10%)中取 ASR 最小;无可行 → refusal 最小并记 `constraint_ok=False`。
   - **协议 §8 的 clean 侧约束**(benign mis-filter≤5% 代理)**未实现**:tune.py 从未计算 mis-filter;`clean_B0_ref` 仅跑 B0 且用**空 gold/attack**(`main_state(out["answer"],"","")`,`tune.py:78-83`)→ clean accuracy 恒 0.0,只作展示。
   - 结果:S0/S1 全 τ 不可行(0.467–0.583);唯一可行族 S2 → chosen = **S2 λ0.2(τ0.0)**(0.633/0.100)。
   - **执行**:G0 = S0@τ**0.05**(若按规则"无可行→refusal 最小"应选 τ0.5,val refusal 0.4667);G1 = S2@λ0.2 **τ0.25**(非 τ0.0)→ **主表 G0/G1 两个操作点都不是 tune.py 规则输出;没有任何文档记录它们是"作者选点"**。
   - B1/B2/B6:`tune_baselines.json` **只有 scan 无 chosen 字段**;主表参数 B2=0.7/B6=0.5 与报告 §6 一致 ✓;报告 §6 说"B1 0.85"但 scan 只见 τ0.1–0.5(全部 ASR 不变 0.85),主表执行 B1 τ_rel=**0.30** → **B1 选点叙述(0.85)与执行(0.30)不一致,且 0.85 点未见记录**。
4. **生成一致性**:服务器重跑(当前代码)test B0 10/10 与主表一致;17:12/13:37 的早期运行与主表同代码(8-25 唯一代码改动=probe_logits,不影响 complete)→ **val 调参与 test 评估生成质量一致**(无"调参用退化生成"问题;与此前疑点 §9 的 20:47 目录无关)。

**分类**:划分 **合规(仅正文 70/30 错)**;选参 **实现错误**(干净约束未实现;执行点≠规则输出;B1 无选中记录)→ 另 **证据不足**(G0/G1 选点动机、B1 0.85 出处均无记录)。

---

## P1-2 自适应训练与评估到底完成到哪一层

**事实链**(`fp/adaptive.py` + 结果文件 v3):

1. **隔离**:训练查询=train split 前 10 条/数据集;评分查询=val split 前 10 条/数据集;不相交 ✓。unseen 族(d/e/f)从未进训练池(`--eval-only` 用 seen 训练的最终 MLP)✓。
2. **轮次**:r0=RuleRisk(基础池打分);r≥1 先训练 MLP(基础池+前轮挖矿逃脱(risk<0.25,τ_escape))再 val 评分;r2 后无新挖矿(pool 88 不变)。
3. **样本构成**:基础池正例 = train 查询原投毒组(adv_texts[:5]);负例 = **normal 文档组,且 impact/control 被强制置 0**(adaptive.py:216-217)→ **训练负例带人工构造的特征(0/0),与评分负例(val 正常组,真实 impact)分布不同**——训练-评估分布偏差【实现缺陷】。
4. **指标层级**:只有**检测器级**(变体组评分 AUC + escape rate);docstring(adaptive.py:12-13)声明的"端到端最终验证:最后一轮检测器+变体语料在 frozen TEST 上跑 B0/G*"——**未找到结果文件、未找到脚本(全库 grep)** → **生成级(端到端)ASR 未完成**;报告 §7"曲线为检测器级;端到端 ASR 主表基于冻结语料"已如实声明(该限制正确)。
5. **数字**:v3 文件与报告 §7 表一致(r0 0.886/0.886 / r1 0.812(escape 0.033,0.000,0.067)/ r2 0.837(0.000,0.000,0.000)/ unseen 0.865(0,0,0));报告 §0"第 0-2 轮与未见过的新变体族上逃脱率均为 0"与 r1 的 0.033/0.067 **不一致**(§7 表一致)→ §0 摘要与 §7 矛盾,**仅正文错误(小)**。
6. **候选答案用 oracle**(correct/incorrect 作为 answers)→ 见 P0-2 第 4 条。

**分类**:检测器级 **已完成且隔离合规**;生成级 = **未找到(应视为未实现,限制已如实在报告 §7)**;r1 escape 0.033/0.067 摘要性表述错误【仅正文错误】;负例人工置零【实现缺陷】。

---

## P1-3 论文中的数字能否逐项找到原始结果

**逐项溯源主表 §2(冻结 test,n=60,seed 20260825)**:

| 论文数字 | 文件 | 状态 |
|---|---|---|
| B0 0.833/0.100/0.017 | eval_test_B0_B1_B2_B4_B5_B6_G0_G1/metrics.json | ✓(重跑 10/10 一致) |
| B1 0.833(τ=0.30) | 同上(参数来自 run_main_eval.sh;config.json **不落盘参数**) | 数字✓,参数可溯源(sh)但 config 未记 |
| B2 0.750(τ=0.70) | 同上 | ✓ |
| B4 0.867 | 同上 | ✓ |
| B5 0.150/0.333/0.483 | 同上(trustrag 复刻) | ✓ |
| B6 0.650(τ=0.50) | 同上 | ✓ |
| G0 0.217/0.333/0.400(τ=0.05) | 同上 | 数字✓;**选点非规则输出(P1-1)** |
| G1 "S2 λ=0.20" 0.583/0.300/0.067 | 同上 | 数字✓;**实际执行 S2@λ0.2+**τ=0.25**(P0-3)** |
| 检索 asr@5=1.00 / avg 3.82 | 同上 retrieval | ✓ |

**clean 表 §3 & §8 Qwen 行**:文件 `eval_clean_val|test_B0_G0_G2`(17:12/17:39,当前代码一致——复算 clean_val S2λ0.2 avg_removed=0.71=predictions ✓)。**但报告 §3 的"G1"行 = 该文件中 "G2"(S2@λ0.2,τ=0.0),与主表 G1(S2@λ0.2,τ=0.25)不是同一操作点** → 表内"G1"标签错配【仅正文/标签错误】;§8 Qwen clean 行同理(G1 的 τ0.25 干净版本在 `eval_clean_val_B0_G1`(20:47)——但其生成行为与当前代码不一致(§9),暂不采用→τ0.25 点无干净数据)。

**其余**:
- §4 exact CCI:H8/H16/H32 val=0.360/0.394/0.386、train H16=0.393 → 对应文件 ✓(val H16 文件内 0.3941;_control/impact 相同因 control≡impact)。
- §5 "val AUC 0.769→0.858、τ=0.05 漏检 0":**未找到源文件**(diag_risks.py/diag_val.py 存在但输出未发现于 results)→ **证据不足**。
- §6 E8:1.36/1.53/1.81x,n=15/8/16=39 → eval_loo_final.json ✓;RuleRisk 0.533/0.350@6.7% → eval_test_G0_G1(rule)✓(规则/MLP 均含 τ0.25 残留,对比同口径→公平)。
- §7 自适应 → adaptive_curve/adaptive_unseen v3 ✓(见 P1-2 第 5 条)。
- §8 跨 LLM:Qwen(主表)✓;Llama/Mistral → tag 目录(eval_test_*_Instruct|v0.3)✓;**注意无 tag 目录 eval_test_B0_G0_G1 已被 Mistral 运行覆盖(数字与 _v0.3 完全相同);该目录曾存的 Qwen 结果随之丢失**——报告未引用它 ✓,但未来引用危险;**根因:①predictions.jsonl 的 model 字段硬编码 "qwen2.5-7b-instruct"(runner.py:286)→无法事后辨别 ②目录 tag 修复前存在覆盖窗口**。

**分类**:主表/E8/exact/跨模型 tag 目录 **可溯源**;两处错配(clean 表 G1 标签、Qwen clean 行方法)+ §5 无源(证据不足)+ 目录覆盖历史(实现缺陷)。

---

## 待核查标记(按你要求,先改这四项前必须确认)

| 项 | 状态 |
|---|---|
| 联盟定义 | 代码事实已确认(is_coalition = size≥2∧(cohesion>0∨impact>0));**论文 M5 卡"三条标准/平均值/阈值"与实现不一致 + "single highly-influential document 也是联盟"与单点豁免矛盾** → 正文改法待定 |
| 外部佐证公式 | 已确认恒 0(定义级);正文 IndepSupp 项与"corroborated benign cluster"叙事需删改;结构性再定义需先设计再做实验 |
| 候选答案构造 | 主管线=query-sim top-2(target-free)✓;**adaptive 用 oracle answers → 待定** |
| S2 规则 | 语义=删除前保留下限检查,无重新加入/重算 ✓;**正文 S2 描述(若含"重新加入/重新计算")需核对——docx 正文未抽取,未找到 → 待核查** |

## 本轮最小修复方案(未执行;均先审后动)

1. **P0-1(机理层)**:论文与协议改口——把 6 特征中的 external_corroboration/control 描述改为"实现中恒 0 / ≡impact,该维度不具判别力"(或直接从特征表删除),删除"外部佐证缺失"判别叙述;若保留三维 Score 需真外部支持设计(新实验,暂缓)。→ **论文先写已实现机制,勿写三维 Score 判定**。
2. **P0-3/P1-1(G1 τ 残留 + 选点)**:①`run_main_eval.sh` 的 G1 覆盖补 `"tau":0.0`(与 tune 点一致)或改 METHODS 合并逻辑,杜绝残留;②决定 G1 报告点:只报"调参点(τ0.0)重跑"或"τ0.25(标注非标点)";③G0 要么按规则在 val 重选(τ0.5,ASR 0.333/ref 0.467)要么明确标注"作者选择的强保护点(协议外)"。重跑量:test G0,G1+clean(G1 补)≈2-3h GPU。
3. **P1-1 clean 约束**:tune.py 增加 mis-filter 复算(或明确降级为"仅 refusal 约束+事后报告 mis-filter≥5% 的部分"),tune 重放(无需 GPU 生成?——mis-filter 只需 selection,无需生成 ✓ 快)。
4. **P0-2/adaptive**:①候选答案改 query-sim top-2(一处改动)重跑 P6(≈20min);②或报告明确"自适应攻击者模拟已知目标"。
5. **P1-2**:报告如实"生成级未做";训练负例 impact/control 置零问题→重训 P6 时用真实值。
6. **P1-3 工具**:runner config.json 落盘 METHODS 生效参数;predictions `model` 字段取实际后端;恢复被覆盖目录(从 tag 目录复制或用日志重建 Qwen eval_test_B0_G0_G1)。
7. **正文**:70/30→60/20/20;M5 三条标准→实现口径;M8 71pp/τ0.25 旧口径数字删除;§0/§7 escape 摘要;报告 §3 G1标签。

**红线遵守**:未启动新实验(仅只读复算+2 次 10-query 诊断重跑);未改任何代码;诊断均基于 train/val 与结果文件;所有"未找到"均如实标注。
