# RAG-ConGuard 项目说明文档

**定位**：面向"协同知识投毒"（多篇互为支撑的投毒文档组成证据联盟、共同支配生成）的联盟感知反事实防御：将检索证据结构化成语义有符号图 → 发现证据联盟 → 测量联盟级反事实控制力 → 用集合级风险学习器重建安全上下文（必要时拒答）。

**数据口径**（重要，防串数据）：本文档全部数字以**冻结协议 v1.2**（`experiments/final_protocol/FINAL_PROTOCOL.md`）与**最终实验报告 v1.0**（`FINAL_EXPERIMENT_REPORT.md`，2026-08-26，服务器 AutoDL 4090 产出，逐 run 落盘可复核）为准。
- 作废口径①：早期"ASR 0.86–0.99 → 0.143 / 拒答 100%"——拒答换 ASR，不得写作"效用保留"；
- 作废口径②：早期 E8 表"1.01×/1.60×/1.98×"（含 oracle 候选答案泄漏）；现行 1.36×/1.53×/1.81×；
- 口径③：`Tables_Figures.md` 的 Table 1–4 为旧 100 查询回收口径（0.14/拒答 1.0 等），仅检索面命中率可作旁证，其余不可混用。
- 报告原则（用户红线）：**不筛数、不修饰；负结果与限制全部保留**（代理-精确影响仅中等相关、规则学习器端到端不弱于 MLP 等均如实写入）。

---

## 1. 研究背景

大语言模型无法记住开放事实，RAG 已成为向 LLM 补充实时/领域知识的主流形式，其上下文取自开放、无策展的语库（公开网页、社区语料等）。**这是双刃剑**：语库无策展 ⇒ 必然含偏差、误导与恶意内容，且被检索到的攻击性段落可以直接把模型染指到注入断言上——这不是质量缺陷，而是安全缺陷。

攻击在不断演化。早期攻击扰动少量文档以误导检索（检索面攻击）；近期攻击进入**协同阶段**：为每个目标查询注入一组近重复文档（本文为每查询 5 篇，同一错误断言、不同表面形式），单篇各自无害，**组级**联合支配检索结果与生成输出。攻击材料注入率极低（500/600,000 文档 ≈ 0.08%）即可奏效；本项目的实测（冻结口径）显示：**top-5 检索 ASR = 1.00，平均 3.82/5 篇为投毒文档**——攻击材料必然到达生成器，删除任意单个成员后其余成员仍然完全控制输出。

现有防御仍停留在"逐文档"范式：似然/困惑度/嵌入距离等手工特征过滤、聚类过滤、NLI 一致性推理、归因信号——全部对单文档打分并依赖静态阈值。本文的实证反例显示这类防御在本威胁下的失效程度：**相关性过滤改善 0.000**（0.833→0.833，阈值 τ=0.30 未命中任何文档）；**重复度过滤仅 -8.3pp 且 95% CI 含 0**（0.750，B2）；**NLI 一致性防御反而倒挂 +3.3pp**（0.867 > 0.833——"内部一致多数"恰恰就是投毒联盟本身，是最强的反例证据）。

问题层层收敛：协同投毒的信号不藏在单篇文档里，而藏在文档间的**结构性与冗余性关系**（组级支配 vs 单篇无辜）里；同时，支配组不等于恶意组——支持正确答案的权威证据同样有强影响力。因此需要一个系统判据，把"**缺乏独立支持、却对生成起支配性控制**"的联盟与"被多方佐证的重要证据"区分开，且不能靠检索侧修复（检索本身已 100% 命中）。

## 2. 科学问题

- **SQ1（科学问题，机理）**：协同投毒下，生成的支配权如何从单文档层面"被稀释"并转移到联盟层面？——组规模增大时，单文档归因是否系统性低估、联盟级归因是否系统性显露，即"稀释机制"是否有可量化形态。
- **SQ2（科学问题，规律判据）**："影响支配力"与"外部独立佐证"能否解耦成可计算的判别准则？——即 **Unsupported Influence（无独立支持的影响力）** 假设：成功协同攻击表现为"支配性控制相对其独立佐证不成比例"的联盟；该假设能否区分恶意联盟与权威联盟、在无目标先验（target-free）条件下成立。
- **TQ1（技术问题）**：在无 oracle 候选答案、无 token 级生成探针预算、top-5 小图（精确可算）的条件下，如何可计算地刻画联盟级反事实控制力，并据以构建集合级安全上下文（清除风险联盟 / 贪婪重建 / 有序拒答），同时显式暴露鲁棒–效用权衡？

三者构成"机理 → 判据 → 方法"的靶心链，S5 的实验结果逐条回扣。

## 3. 创新点（≤3 条）

1. **【理论/机理】"无独立支持的影响力"判别准则与联盟级反事实控制度量**：提出将协同投毒建模为查询条件有符号图上的证据联盟，并定义联盟级反事实影响力 I(C)（信任加权答案支持在删除整个联盟后的相对下降，加权 1−外部佐证）。该度量只依赖证据侧计算、无需梯度或生成探针，把"投毒信号在组不在单篇"的假设转化为可计算判据。
2. **【方法】集合级风险学习 + 风险约束安全上下文重建流水线**：建立"有符号图 → 联盟发现（精化：size≥2 且 cohesion/impact >0，单点豁免）→ 6 维集合级特征风险模型（MLP 与规则双实现）→ 风险约束选择器（S0 整联盟剔除 / S2 贪婪重建）"，并以"强保护点 + 可用性点"两个操作点显式呈现鲁棒–效用前沿，而非用单一数字隐藏权衡。
3. **【应用/方法】联盟感知自适应攻击与对抗在环训练**：设计互证/去协调/混合三模板生成变体，以"当前风险模型打分 → 逃脱样本入难例池 → 重训 → 再定向"的对抗在环调度训练防御，并用**未见过变体族**评测泛化（escape 0.00）；为知识投毒防御提供对抗在环评测范式。

## 4. 研究方法与技术路线

**方法体系**：假设驱动 + 对照实验 + 消融验证；图论（正边连通分量、信任传播迭代精化）、NLI 语义三分类 + 数值冲突启发式 + LLM 兜底融合、集合级监督学习（sklearn MLP(16,8)+StandardScaler，仅拟合 train）、端到端生成评测（逐查询互斥主状态）+ 配对 bootstrap（1000 次）统计。

**材料与仪器**：AutoDL RTX 4090；BGE-base-en-v1.5（FAISS cosine）+ BM25（RRF 融合 top-5）；cross-encoder/nli-deberta-v3-base（MNLI，阈值 0.7）+ LLM judge（α=0.3 融合）；生成器 Qwen2.5-7B-Instruct（fp16，T=0，128 tok），扩展 Llama-3.1-8B-Instruct、Mistral-7B-Instruct-v0.3。数据：BEIR NQ/HotpotQA/MS MARCO 各 600k 语料、3,452/5,000/5,000 查询；PoisonedRAG 式定向投毒（100 查询 ×5 近重复文本，注入率 0.08%）；**冻结 split**（seed 20260825：投毒 60/20/20 分层、clean 200/100/300 每数据集，交验无重叠；算法调参仅用 val：拒答 ≤10% 约束）。

**技术路线（问题→缺口→方案→成果的逻辑闭环）**：① 检索面归因（ASR@5=1.00，证明问题存在于证据层、防御必须前置到生成前）→ ② 机理刻画（E8 联盟 vs 单篇归因 1.36×→1.81×，证明稀释机制、驱动联盟级设计）→ ③ 判别验证（学习器 val AUC 0.858 vs 规则 0.32；单点豁免消融 0.769，证明判别力来自结构特征组合）→ ④ 端到端（主表 G0/G1 双操作点 vs 全部逐文档基线，含 B4 倒挂、B1 零改善反例）→ ⑤ 保护面（clean 侧拒答/误滤 + 3 LLM 泛化）→ ⑥ 进化（自适应攻击 × 在环训练，unseen 逃脱 0）→ ⑦ 诚实校验（exact-CCI ρ≈0.39，把代理的边界写为分析对照）。

### 4.1 关键步骤详解：协同投毒如何被"认定"（三层口径）

**第一层：实验真相层（仅作标注与评估，推理期禁用）。** 投毒文档由 PoisonedRAG 式注入生成：每查询 5 篇近重复 adversarial texts，doc_id 带 `:poisoned:` 标记；ground truth 只用于①训练弱标签——联盟投毒比 poisoned_ratio ≥ 0.8 标正例、clean_trn 查询联盟为负例；②指标核算（AUC、E8 按组规模分组、ASR）。协议 v1.1 曾修复一处推理期 oracle 泄漏：旧实现以 benchmark 的 correct/incorrect answer 作影响公式的候选答案，现改为"与查询 BGE 最相似的 top-2 claim"（target-free）。

**第二层：推理层（防御运行时的判定链）。** 逐一查询执行：

```
检索 top-5 → 220 字符 claim → 有符号图（NLI 三分类 th=0.7 + 数值冲突启发式 + LLM judge α=0.3）
→ 联盟发现：正边连通分量；精化 v1.2 = size≥2 ∧ (cohesion>0 ∨ impact>0)（孤立单点豁免——
   单点的 6 维特征恒为 0，投毒/良性单点无法区分，故不算联盟、不可删除）
→ 每个联盟计算 6 维特征：size / cohesion / external_corroboration / external_refute / impact / control
→ 风险分数：MLP(16,8)+StandardScaler（或规则加权 sigmoid，双实现），scaler 只 fit train
→ risk ≥ τ → 剔除该联盟（S0 整联盟剔除；S1 仅删最高风险；S2 按 λ 保留下限贪婪重建）→ 全剔则拒答
```

要点：①"条件"并非手工预设规则，而是学到的风险分数过阈值（MLP/规则两实现，见 §5-R5）；②处置是**集合级**的——以联盟为删除单位，对应"信号在组、不在单篇"的假设，与逐文档 LOO/过滤有本质区别；③τ/λ 仅在 val 扫描（拒答 ≤10% 约束），test 只应用一次。

**第三层：评价层（事后回看证据）。** 逐查询互斥主状态（attack-success/correct/refusal/other → ASR/Acc/Refusal；拒答另报，不从分母剔除），AUC 度量风险分数对真假联盟的排序力（val 0.858 vs 规则 0.32，见 §5-R5），paired bootstrap 1000 次判显著性。

**"什么证据被判为高风险"＝ Unsupported Influence 判据。** 高风险 ⟺ 联盟同时满足：内部自洽（正边连通、cohesion 高）+ 外部无独立佐证（external_corroboration 低）+ 移除它后答案支持分大幅下滑（impact 高）——control = impact × (1−corr) 即三者合一。两个对照案例：①投毒近重复簇：cohesion≈1、外部零佐证、impact=0.944（4 篇组，§5-R4），control≈0.944 → τ=0.05 下剔除；当 58% 查询的整个 top-5 被单一联盟控制时，剩余证据为空 → 拒答是唯一诚实动作（G0 的 40% 拒答由此而来）。②良性多源簇（支持正确答案的共识证据）：同样连通、同样有影响力，但被外部独立佐证支撑 → (1−corr) 压低 control → risk 低于 τ → 保留。由此可直接解释 B4 NLI-一致性基线倒挂（0.867 > 0.833）：它把"内部一致多数"当安全信号，而协同投毒恰恰构造了内部一致。

**辨析**：防御运行时不输出"投毒"标签，只输出证据置信分数——判据是"支配力 vs 外部佐证"的分离，而非"恶意 vs 权威"；即使攻击者公开知道该判据（Score=α·Density+β·I−γ·Supp），在只控制语料 ~0.08% 的前提下也无法造出"既支配、又有独立佐证"的联盟。

## 5. 实验与结果 ★（重点）

> 默认设定：冻结 test 60 查询（每数据集 20）；所有对照（基线/防御）共享同一检索器、语料、攻击与生成器；每查询标记互斥主状态（attack-success/correct/refusal/other）。下表为主表结果（表 1，n=60）。

| 方法（基线/操作点） | ASR | Acc | Refusal |
|---|---|---|---|
| B0 Vanilla RAG | 0.833 | 0.100 | 0.017 |
| B1 相关性过滤（τ=0.30） | 0.833 | 0.100 | 0.017 |
| B2 重复度过滤（τ=0.70） | 0.750 | 0.200 | 0.017 |
| B4 NLI-一致性 | 0.867 | 0.083 | 0.033 |
| B5 TrustRAG（原版复刻） | 0.150 | 0.333 | 0.483 |
| B6 逐文档 LOO（τ=0.50） | 0.650 | 0.217 | 0.067 |
| **G0 强保护（τ=0.05）** | **0.217** | 0.333 | 0.400 |
| **G1 可用性（λ=0.20）** | **0.583** | 0.300 | 0.067 |

**R1｜检索面攻击密度 ✅（可直接写入论文）**
- 条件下：B0 无防御，冻结 test。
- 数据：retrieval ASR@5=1.00；top-5 平均投毒 3.82 篇；旁证（旧 100q 口径）：dense 视图命中 0.98–1.00、lexical 0.86–1.00。
- 支撑：SQ1/TQ1 的前提——攻击 100% 到达 prompt，任何"只改检索"的防御都无效，防御必须做证据结构层。

**R2｜主结果（✅ 核心）**
- 条件：冻结 test，基线同池同参数；paired bootstrap（1000 次，vs B0）。
- 数据：G0 ASR 0.833→0.217（Δ=-0.617，95%CI [-0.750,-0.483]）；G1 0.583（Δ=-0.250，CI [-0.400,-0.117]）；B6 0.650（Δ=-0.183，CI [-0.300,-0.083]）；B5 0.150（Δ=-0.683，CI [-0.817,-0.533]）但拒答 48.3%；B2 Δ=-0.083（CI [-0.183,0.017]，不显著）；B4 Δ=+0.033（倒挂）。
- 支撑：SQ2+TQ1 的端到端验证——联盟级在**同 6.7% 拒答层位**全面优于逐文档 LOO（0.583/0.300 vs 0.650/0.217）；逐文档体系（含一致的"多数一致性"）失效或倒退；TrustRAG 数值更低但机制是 LLM 自评估**改写答案**（证据层入侵），与证据选择不可直接比，论文如实并陈。

**R3｜干净侧效用 ✅**
- 条件：clean_test 900 查询（300×3，无投毒）；B0 自拒答为锚点。
- 数据：B0 clean refusal 0.286；G0 0.319（+3.3pp）/ 良性误滤 8.7%；G1 0.308（+2.2pp）/ 13.5%（均 <15% 预算）；val 面 G0/G1 分别为 0.317/0.106、0.293/0.144。
- 支撑：防御代价可量化且预算内。⚠️ 注：BEIR UKP zip 无 gold 答案，**clean accuracy 不可得**（已实拉核验），论文必须用"拒答 + 误滤"两手作效用口径。

**R4｜归因机制（E8）✅（动机核心，弱机制定量）**
- 条件：test 39 个 ≥2 投毒组，同一影响力公式重放：单篇 LOO 删除 vs 整联盟删除。
- 数据：组规模 2→ 0.353/0.480（1.36×）；3→ 0.466/0.714（1.53×）；4→ 0.521/0.944（1.81×）；单篇解码 2→4 仅 +0.168，联盟级 +0.464。
- 支撑：**SQ1 稀释机制的定量证据**——单篇归因被剩余成员系统性稀释，且随组规模放大；旧 oracle 口径（1.01×→1.98×）已作废，新口径趋势一致。

**R5｜风险学习器判别能力 ✅（含诚实不利面）**
- 数据：val AUC 0.858（train 0.857）；规则基线 AUC ≈0.32；移除"单点豁免"→ 0.769（无豁免时联盟发现退化为逐文档异常）；端到端：**规则风险 0.533/0.300 vs MLP 0.583/0.300（@6.7% 拒答），规则略优于 MLP**。
- 支撑：SQ2——判别力主要来自"6 特征结构组合"本身而非学习器容量；学习器价值在自动化与校准（AUC 0.79–0.86 vs 0.32）。✅ 论文已如实写"规则与学习器为同一结构信号的两个实现"。
- 🔬 需补充：逐特征重要性/组合消融，可在改稿期补做以强化特征选择的合理性。

**R6｜自适应攻防（检测器级）✅**
- 条件：三模板变体 × 在环训练调度；评估=变体组风险分数的逃脱率与判别 AUC。
- 数据：r0 规则 A UC 0.886 / escape 0.00；r1 MLP AUC 0.812 / escape 0.033–0.067（部分绕过）；r2 MLP AUC 0.837 / escape 0.00；**unseen 变体族（d/e/f，从未进训练）AUC 0.865 / escape 0.00**。
- 支撑：静态"训练一次后评测"面对进化攻击不足——r1 的 0.033–0.067 绕过即证据；在环训练后 r2 与 unseen 均重回 0 逃脱。
- 🔬 需补充：端到端生成级自适应 ASR 曲线（当前为检测器级评测）；"对抗在环训练即对抗训练"的跨攻击族验证。

**R7｜跨 LLM 泛化 ✅**
- 数据：B0 ASR Qwen 0.833 / Llama 0.650（天然更抗）/ Mistral 0.883（最脆弱）；G0 → 0.217 / 0.250 / 0.300；G1 → 0.583 / 0.550 / 0.650；clean mis-filter 13.5% / 7.1% / 7.1%。
- 支撑：防御作用于证据集结构与选择，而非模型特性（易攻排名在防御后保持，绝对 ASR 全员下降）。

**R8｜精确 CCI 对照（诚实核心限制）✅ 作限制证据**
- 条件：teacher-forced 同前缀对齐的 exact CCI（full vs D∖C 两条件，L×H token 探测 JSD）；Spearman vs 代理 impact/control（两代理互为单调映射，等价）。
- 数据：val H=8/16/32 → ρ=0.360/0.394/0.386；train H=16（n=99）→ ρ=0.393；**稳定、H 不敏感、中等相关**；小规模扰动大（n=3–16 时 ρ∈[-0.5,+0.7]）。
- 支撑：代理 CCI 是有效排序信号但**非强代理**——论文 M6 表述必须为"证据侧近似 + exact-CCI 分析对照"，不得声称代理=生成 JSD。
- 🔬 冲击二区的关键缺口正好在此：需廉价代理（蒸馏/短探针）拉高 ρ，或直接全生成探针。

**R9｜选择器与 τ/λ 敏感度 ✅**
- 数据（val 60q 平面）：S0 整联盟剔除 τ∈[0.05,0.5] → ASR 0.167–0.333 / refusal 0.467–0.583；S2 贪婪重建 λ∈[0.2,1.0] → ASR 0.567–0.850 / refusal 0.000–0.133；graph 变体 signed_semantic 在 S0 上 ASR 更低（0.100）但拒答 70.0% vs signed 58.3%（保留 signed 主链，semsem 作消融）。
- 支撑：鲁棒–效用是**平滑前沿**而非脆点；在 3.82/5 攻击密度下（**58% 查询整个 top-5 被单一联盟控制**）"ASR≥70pp 降幅 + 拒答≤10%"不可兼得——这是攻击密度的固有代价面，论文用双操作点如实呈现而非隐藏。G0 的 40% 拒答是正确动作（无可信子集可回答），不是缺陷。

## 6. 研究贡献

**理论贡献**：① 提出"Unsupported Influence"刻画框架（支配性控制 vs 独立佐证解耦，target-free）；② 给出协同投毒下"逐文档归因稀释"的可量化规律（1.36×→1.81×，随组规模放大）；③ 指出"NLI 多数一致性"在协同投毒下失效/倒挂的机理（0.867>0.833）——为该威胁态提供判别理论反例。

**方法贡献**：RAG-ConGuard 完整流水线（有符号图 + 联盟精化 + 代理 CCI + 6 特征集合级风险模型 [MLP/规则双实现] + S0/S2 风险约束选择器）+ 联盟感知自适应攻击与对抗在环训练调度；全部代码、冻结协议（split/指标/统计口径）、逐 run 落盘结果**完全可复现**（本地=源、服务器 md5 一致）；形成初稿：AAMAS 2026 类章节结构英文正文（46 卡）+ 4 张真实数据表。

**应用贡献**：在 NQ/HotpotQA/MS MARCO × 3 LLM 上建立可复制的防御基准（含 TrustRAG 原版公平复刻、LOO、NLI-一致性等 6 基线，含 B5 机制差异辨析）；给出"强保护/可用性"双操作点选档方法（τ/λ 网格 + val 约束）；提供对抗在环防御评测范式（unseen 泛化 escape 0.00）。

**预期成果形式**：论文 1 篇（目标 CCF-B/SCI 二区；内部 Level 判定：SCI 二区谨慎乐观达标、三区确定可投、CCF-B 未达——缺口见 §5-R8/R5/R6 的 🔬 项）+ 开源代码 + 公开基准结果。当前价值主张：**强基线集合全面、清楚效用可量化、三模型方向一致、自适应 unseen 泛化、限制如实**（B4 倒挂与 E8 稀释 1.36→1.81× 是核心论证）。

## 7. 局限性

1. **影响度量是证据侧代理**：代理-精确 CCI 仅中等相关（ρ≈0.39，n=99）；论文已降级为"近似 + 对照"。
2. **学习器端到端增益有限**：规则风险 (0.533) 略优于 MLP (0.583)@6.7% 拒答；学习器价值集中在判别 AUC 与自动化。
3. **clean accuracy 不可得**：BEIR UKP zip 无 gold 答案，utility 用"拒答 + 良性误滤"代理，无正确率损失的直接度量。
4. **攻击面单一且为合成**：PoisonedRAG 式近重复定向投毒（注入率 0.08%）；未测真实污染语料、跨文档语义拼接、门控/后门类攻击；自适应族均保留断言、改写风格，未涵盖语义反转/混入良性来源的更强变体。
5. **自适应评测为检测器级**；无端到端生成级攻防曲线；对抗在环训练未推广为泛攻击族对抗训练。
6. **规模与实验条件**：冻结 test 每数据集 20 查询（共 60）；exact-CCI n=99 且小规模扰动大；单台 4090；仅 7–8B 级生成器；KG 检索视角关闭（评估口径 `--no-kg`，多视角实为双视图）；无延迟/成本测量（NLI 两两判定 + LLM 兜底的开销未量化）。

## 8. 未来展望

1. **代理增强**：将 exact-CCI 蒸馏为轻量打分（或 logit 短探针）以抬高 ρ；对全生成探针做成本-收益分析；把影响度为因果性标量并做干预检验。
2. **攻防深化**：纳入语义反转、良性来源混合、Joint-GCG 类门控攻击；构建端到端生成级自适应 ASR 曲线；将"在环困难例挖掘"发展为对抗训练型防御。
3. **检索-防御协同**：与鲁棒检索/检测器（如 PoisonedRAG 检索侧）联合，把联盟特征前馈至重排，降低对"检索 100% 命中投毒"场景的依赖。
4. **评测面扩展**：补齐 gold 答案（拉取 qas）得到真正 clean accuracy；扩展至更多基准与真实污染语料；探索 k>5 时的近似图上方法（大图无法精确枚举，需图采样/超图聚合）。
5. **理论化**：联盟控制与生成分布的因果性、攻防博弈的均衡/对抗训练稳定界、可证明的"拒答-鲁棒"权衡界。
6. **工程化**：延迟优化（NLI 矩阵批处理/缓存）、S2 选择器与答案级改写（TrustRAG 类）组合为"证据选择 + 回答校正"双层方案，向真实 RAG 服务部署。
